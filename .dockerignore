# Rust build artifacts
/target

# Git
.git
.gitignore

# IDE / editor
.claude
.omc
.vscode
.idea

# Leftover SQLite file from the pre-Postgres version
bulb.db

# Docs & misc
README.md
*.postman_collection.json

# ESP8266 firmware (not needed for the server)
esp8266/

# CI config — not needed inside the image
.github

# Docker
Dockerfile
.dockerignore
docker-compose*.yml

# NOTE: migrations/ must NOT be ignored — refinery embeds them at compile time.
